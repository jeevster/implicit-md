includes:
- configs/md17/train.yml
train: False
bond_dev_tol: 2.0
max_frac_unstable_threshold: 0.99 #decay this?
temperature: 500.
steps: 2000 # 1 picosecond per epoch
timestep: 0.5
n_replicas: 1 #can do more at inference?
n_dump: 100
n_epochs: 300 #corresponding to 300 ps

